// 指针操作测试用例
// 测试功能：
// 1. 指针声明：int *p;
// 2. 取地址操作：p = &a;
// 3. 解引用赋值：*p = 123;
// 4. 解引用读取：int x = *p;

int main() {
    int a = 1;
    int *p;
    p = &a;
    *p = 123;
    return a;  // 应该返回 123，因为通过指针修改了 a 的值
}
